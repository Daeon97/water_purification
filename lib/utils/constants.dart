import 'dart:ui';

const baseColor = Color(
  0xFF357ED4,
);
const baseColorInactive = Color(
  0xFF1A1A1A,
);
const timerTextColor = Color(
  0xFF909090,
);

const paramsField = 'params';
const waterLevelField = 'water_level';
const topValveField = 'top_valve';
const bottomValveField = 'bottom_valve';
const timerField = 'timer';

const liquidDownloadRiveAsset = 'assets/liquid_download.riv';
const timerRiveAsset = 'assets/timer.riv';

const artboard = 'Artboard';

const liquidDownloadStateMachineName = 'Guido SM';

const downloadingSMIInput = 'Downloading';
const progressSMIInput = 'Progress';

const indeterminateAnimation = 'Indeterminate';
const resetAnimation = 'Reset';

const animationOne = 'Animation 1';
